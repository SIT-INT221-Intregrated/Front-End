version: "3"
services:
    frontend:
        build: 
         context: .
        image: frontend
        ports: 
            - "8080:8080"
        networks:
            - intregrated-network
networks:
    intregrated-network:
       external: true

